// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVPEL-R32-R32
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovpel R1:R32, R2:R32,  .Typedoperands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> ((#ifMInt (eqMInt(concatenateMInt(mi(63, 0), getFlag("PF", RSMap)), mi(64, 0)) ) #then ( concatenateMInt(mi(32, 0), extractMInt(getParentValue(R2, RSMap), 32, 64)) ) #else ( concatenateMInt(mi(32, 0), extractMInt(getParentValue(R1, RSMap), 32, 64)) ) #fi)    )


)

    </regstate>
endmodule

module CMOVPEL-R32-R32-SEMANTICS
  imports CMOVPEL-R32-R32
endmodule
/*
TargetInstr:
cmovpel %ecx, %ebx
RWSet:
maybe read:{ %ecx %pf }
must read:{ %ecx %pf }
maybe write:{ %rbx }
must write:{ }
maybe undef:{ }
must undef:{ }
required flags:{ cmov }

Circuit:
circuit:callq .move_064_032_rbx_r10d_r11d  #  1     0    5      OPC=callq_label
circuit:cmovpol %r10d, %ecx                #  2     0x5  4      OPC=cmovpol_r32_r32
circuit:xchgl %ebx, %ecx                   #  3     0x9  2      OPC=xchgl_r32_r32
BVF:
WARNING: No live out values provided, assuming { }
WARNING: No def in values provided; assuming { %mxcsr::rc[0] }
Target

cmovpel %ecx, %ebx

  maybe read:      { %ecx %pf }
  must read:       { %ecx %pf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Circuits:

%rbx   : 0x0₃₂ ∘ ((%pf ? 0x1₆₄ : 0x0₆₄) = 0x0₆₄ ? %rbx[31:0] : %rcx[31:0])

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv

*/