// Autogenerated from templates.
requires "x86-configuration.k"
requires "x86-flag-checks-syntax.k"

/*@ This file defines the semantics of following 51 base instructions.
  -  cmoveq_r64_r64
*/

module CMOVE-R64-R64
  imports X86-CONFIGURATION
  imports X86-FLAG-CHECKS-SYNTAX

  // Unroll START
Unroll START PATTERN-1
  rule <k> 
    execinstr ( cmoveq , (%REG64 ,  (%REG64 , .Typedoperands)) ) => .
  ...</k>
Unroll END
  // Unroll END

  // Unroll START
Unroll START PATTERN-5
  rule <k> 
    execinstr ( cmoveq , (%REG64_1 ,  (%REG64_2 , .Typedoperands)) ) => .
  ...</k>
  <zf> ZI </zf>
  requires zeroMInt(ZI)
Unroll END
  // Unroll END

  // Unroll START
Unroll START PATTERN-5
  rule <k> 
    execinstr ( cmoveq , (%REG64_1 ,  (%REG64_2 , .Typedoperands)) ) => 
      updateReg(MI, %REG64_2)
  ...</k>
  <REG64_1> MI </REG64_1>
  <zf> ZI </zf>
    requires  notBool zeroMInt(ZI)
Unroll END
  // Unroll END

endmodule

module CMOVE-SEMANTICS
  imports CMOVE-R64-R64
endmodule
