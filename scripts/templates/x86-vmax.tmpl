// Autogenerated from templates.
requires "x86-semantic-utils.k"

/*@ This file defines the semantics of following 51 base instructions.
  - vmaxpd_ymm_ymm_ymm, vmaxps_ymm_ymm_ymm,
*/

module VMAXPD-YMM-YMM-YMM
  imports X86-SEMANTIC-UTILS

  // Unroll START
Unroll START PATTERN-15
  rule <k>
    execinstr ( maxpd , %YMM ,  %YMM , DestR, .Typedoperands) =>
      execinstr( maxpd, DestR, 
      joinVector(operateOnVector(splitVector(MI, 64), splitVector(MI, 64), vfmax, 64)), 
      .Typedoperands)
  ...</k>
  <YMM> MI </YMM>
Unroll END
  // Unroll END

  // Unroll START
Unroll START PATTERN-16
  rule <k>
    execinstr ( maxpd , %YMM1 ,  %YMM2 , DestR, .Typedoperands) =>
      execinstr( maxpd, DestR, 
      joinVector(operateOnVector(splitVector(MI1, 64), splitVector(MI2, 64), vfmax, 64)), 
      .Typedoperands)
  ...</k>
  <YMM1> MI1 </YMM1>
  <YMM2> MI2 </YMM2>
Unroll END
  // Unroll END

  rule <k> execinstr(maxpd, DestR:Ymm, MIsrc:MInt, .Typedoperands ) => updateReg(MIsrc, DestR) ...</k>
endmodule

module VMAXPS-YMM-YMM-YMM
  imports X86-SEMANTIC-UTILS

  // Unroll START
Unroll START PATTERN-15
  rule <k>
    execinstr ( maxps , %YMM ,  %YMM , DestR, .Typedoperands) =>
      execinstr( maxps, DestR, 
      joinVector(operateOnVector(splitVector(MI, 32), splitVector(MI, 32), vfmax, 32)), 
      .Typedoperands)
  ...</k>
  <YMM> MI </YMM>
Unroll END
  // Unroll END

  // Unroll START
Unroll START PATTERN-16
  rule <k>
    execinstr ( maxps , %YMM1 ,  %YMM2 , DestR, .Typedoperands) =>
      execinstr( maxps, DestR, 
      joinVector(operateOnVector(splitVector(MI1, 32), splitVector(MI2, 32), vfmax, 32)), 
      .Typedoperands)
  ...</k>
  <YMM1> MI1 </YMM1>
  <YMM2> MI2 </YMM2>
Unroll END
  // Unroll END

  rule <k> execinstr(maxps, DestR:Ymm, MIsrc:MInt, .Typedoperands ) => updateReg(MIsrc, DestR) ...</k>
endmodule


module VMAX-SEMANTICS
  imports VMAXPD-YMM-YMM-YMM
  imports VMAXPS-YMM-YMM-YMM
endmodule
