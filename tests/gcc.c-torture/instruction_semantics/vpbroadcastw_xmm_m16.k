// Autogenerated using stratification.
requires "x86-configuration.k"

module VPBROADCASTW-XMM-M16
  imports X86-CONFIGURATION

  context execinstr(vpbroadcastw:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (vpbroadcastw:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      loadFromMemory( MemOff, 16) ~>
      execinstr (vpbroadcastw memOffset( MemOff), R2:Xmm,  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem16:MInt):MemLoadValue ~> execinstr (vpbroadcastw:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(128, 0), concatenateMInt( Mem16, concatenateMInt( Mem16, concatenateMInt( Mem16, concatenateMInt( Mem16, concatenateMInt( Mem16, concatenateMInt( Mem16, concatenateMInt( Mem16, Mem16))))))))
      )
    </regstate>
endmodule
