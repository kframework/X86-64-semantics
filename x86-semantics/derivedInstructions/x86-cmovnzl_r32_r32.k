// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVNZL-R32-R32
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovnzl R1:R32, R2:R32,  .Typedoperands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> ((#ifMInt (eqMInt(getFlag("ZF", RSMap), mi(1, 0)) ) #then ( concatenateMInt(mi(32, 0), extractMInt(getParentValue(R1, RSMap), 32, 64)) ) #else ( concatenateMInt(mi(32, 0), extractMInt(getParentValue(R2, RSMap), 32, 64)) ) #fi)  )


)

    </regstate>
endmodule

module CMOVNZL-R32-R32-SEMANTICS
  imports CMOVNZL-R32-R32
endmodule
/*
TargetInstr:
cmovnzl %ecx, %ebx
RWSet:
maybe read:{ %ecx %zf }
must read:{ %ecx %zf }
maybe write:{ %rbx }
must write:{ }
maybe undef:{ }
must undef:{ }
required flags:{ cmov }

Circuit:
circuit:cmoveq %rbx, %rcx  #  1     0    4      OPC=cmoveq_r64_r64
circuit:movl %ecx, %ebx    #  2     0x4  2      OPC=movl_r32_r32
BVF:
WARNING: No live out values provided, assuming { }
WARNING: No def in values provided; assuming { %mxcsr::rc[0] }
Target

cmovnzl %ecx, %ebx

  maybe read:      { %ecx %zf }
  must read:       { %ecx %zf }
  maybe write:     { %rbx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Circuits:

%rbx   : 0x0₃₂ ∘ (%zf ? %rbx[31:0] : %rcx[31:0])

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv

*/