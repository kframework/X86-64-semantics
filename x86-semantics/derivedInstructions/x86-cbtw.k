// Autogenerated using stratification.
requires "x86-configuration.k"

module CBTW
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cbtw  .Typedoperands) => .
  ...</k>
    <regstate> ...
 "RAX" |-> ( MI554 => plugInMask(MI554, zeroExtend(extractMask(extractMask(plugInMask(MI550, concatenateMInt(mi(64, 16), signExtend(extractMask(MI554, 8, 0), 64)), 0), 128, 0), 32, 0), 32), 0) )

 "YMM1" |-> (MI550 => MI550)


    ...</regstate>
endmodule

module CBTW-SEMANTICS
  imports CBTW
endmodule
