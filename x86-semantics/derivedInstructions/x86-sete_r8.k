// Autogenerated using stratification.
requires "x86-configuration.k"

module SETE-R8
  imports X86-CONFIGURATION

  rule <k>
    execinstr (sete R1:R8,  .Typedoperands) => .
  ...</k>
    <regstate> ...
convToRegKeys(R1) |-> ( _ => mi(64, uvalueMInt(MI652)) )

 "ZF" |-> (MI652 => MI652)


    ...</regstate>
endmodule

module SETE-R8-SEMANTICS
  imports SETE-R8
endmodule
