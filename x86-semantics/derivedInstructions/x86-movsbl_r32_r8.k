// Autogenerated using stratification.
requires "x86-configuration.k"

module MOVSBL-R32-R8
  imports X86-CONFIGURATION

  rule <k>
    execinstr (movsbl R1:R8, R2:R32,  .Typedoperands) => .
  ...</k>
    <regstate> ...
convToRegKeys(R1) |-> (MI556 => MI556)

convToRegKeys(R2) |-> ( _ => concatenateMInt(extractMask(plugInMask(MI539, mi(32, 0), 0), 32, 0), extractMask(signExtend(extractMask(MI556, 8, 0), 64), 32, 0)) )

 "R11" |-> (MI539 => MI539)


    ...</regstate>
endmodule

module MOVSBL-SEMANTICS
  imports MOVSBL-R32-R8
endmodule
  