// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVNOW-R16-R16
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovnow R1:R16, R2:R16,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> ((#ifMInt (eqMInt(xorMInt(mi(1, 1), getFlag("OF", RSMap)), mi(1, 0)) ) #then ( getParentValue(R2, RSMap) ) #else ( concatenateMInt(extractMInt(getParentValue(R2, RSMap), 0, 48), extractMInt(getParentValue(R1, RSMap), 48, 64)) ) #fi)  )


)

    </regstate>
endmodule

module CMOVNOW-R16-R16-SEMANTICS
  imports CMOVNOW-R16-R16
endmodule
/*
TargetInstr:
cmovnow %cx, %bx
RWSet:
maybe read:{ %cx %of }
must read:{ %cx %of }
maybe write:{ %bx }
must write:{ }
maybe undef:{ }
must undef:{ }
required flags:{ cmov }

Circuit:
circuit:callq .set_sf      #  1     0    5      OPC=callq_label
circuit:cmovngew %cx, %bx  #  2     0x5  4      OPC=cmovngew_r16_r16
BVF:
WARNING: No live out values provided, assuming { }
WARNING: No def in values provided; assuming { %mxcsr::rc[0] }
Target

cmovnow %cx, %bx

  maybe read:      { %cx %of }
  must read:       { %cx %of }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Circuits:

%rbx   : (concat <%rbx|64>[63:16] (if (== (if <%of> then <0x0|1> else <0x1|1>) <0x1|1>) then <%rcx|64>[15:0] else <%rbx|64>[15:0]))

sigfpe  : <sigfpe>
sigbus  : <sigbus>
sigsegv : <sigsegv>

*/