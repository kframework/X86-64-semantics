// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVNLL-R32-R32
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovnll R1:R32, R2:R32,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(32, 0), (#ifMInt eqMInt( xorMInt( (#ifMInt eqMInt(getFlag("SF", RSMap), mi(1,1)) #then mi(1, 1) #else mi(1, 0) #fi), (#ifMInt eqMInt(getFlag("OF", RSMap), mi(1,1)) #then mi(1, 1) #else mi(1, 0) #fi)), mi(1, 1)) #then extractMInt( getParentValue(R2, RSMap), 32, 64) #else extractMInt( getParentValue(R1, RSMap), 32, 64) #fi))
)

    </regstate>
endmodule

module CMOVNLL-R32-R32-SEMANTICS
  imports CMOVNLL-R32-R32
endmodule
