// Autogenerated using stratification.
requires "x86-configuration.k"

module SETNBE-R8
  imports X86-CONFIGURATION

  rule <k>
    execinstr (setnbe R1:R8,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R1) |-> concatenateMInt( extractMInt( getParentValue(R1, RSMap), 0, 56), (#ifMInt eqMInt( concatenateMInt( (#ifMInt eqMInt(getFlag("CF", RSMap), mi(1,1)) #then mi(24, 1) #else mi(24, 0) #fi), (#ifMInt eqMInt(getFlag("ZF", RSMap), mi(1,1)) #then mi(8, 1) #else mi(8, 0) #fi)), mi(32, 0)) #then mi(8, 1) #else mi(8, 0) #fi))
)

    </regstate>
endmodule

module SETNBE-R8-SEMANTICS
  imports SETNBE-R8
endmodule
