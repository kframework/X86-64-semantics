// Autogenerated using stratification.
requires "x86-configuration.k"

module SETNS-R8
  imports X86-CONFIGURATION

  rule <k>
    execinstr (setns R1:R8,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R1) |-> concatenateMInt( extractMInt( getParentValue(R1, RSMap), 0, 56), (#ifMInt eqMInt( (#ifMInt eqMInt(getFlag("SF", RSMap), mi(1,1)) #then mi(64, 1) #else mi(64, 0) #fi), mi(64, 0)) #then mi(8, 1) #else mi(8, 0) #fi))
)

    </regstate>
endmodule

module SETNS-R8-SEMANTICS
  imports SETNS-R8
endmodule
