// Autogenerated using stratification.
requires "x86-configuration.k"

module VPMOVZXDQ-XMM-M64
  imports X86-CONFIGURATION

  context execinstr(vpmovzxdq:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
          rule <k>
            execinstr (vpmovzxdq:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
              loadFromMemory( MemOff, 64) ~> execinstr (vpmovzxdq memOffset( MemOff), R2:Xmm,  .Operands)
          ...</k>
          
  rule <k>
    memLoadValue(Mem64:MInt):MemLoadValue ~> execinstr (vpmovzxdq:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(128, 0), concatenateMInt( concatenateMInt( mi(32, 0), extractMInt( Mem64, 0, 32)), concatenateMInt( mi(32, 0), extractMInt( Mem64, 32, 64))))
      )
      </regstate>
endmodule
