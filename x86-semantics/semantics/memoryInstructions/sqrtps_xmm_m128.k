// Autogenerated using stratification.
requires "x86-configuration.k"

module SQRTPS-XMM-M128
  imports X86-CONFIGURATION

  context execinstr(sqrtps:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (sqrtps:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      loadFromMemory( MemOff, 128) ~>
      execinstr (sqrtps memOffset( MemOff), R2:Xmm,  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem128:MInt):MemLoadValue ~> execinstr (sqrtps:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( extractMInt( getParentValue(R2, RSMap), 0, 128), concatenateMInt( sqrt_single(extractMInt( Mem128, 0, 32)), concatenateMInt( sqrt_single(extractMInt( Mem128, 32, 64)), concatenateMInt( sqrt_single(extractMInt( Mem128, 64, 96)), sqrt_single(extractMInt( Mem128, 96, 128))))))
      )
    </regstate>
endmodule
