// Autogenerated using stratification.
requires "x86-configuration.k"

module ROLL-M32-ONE
  imports X86-CONFIGURATION

  context execinstr(roll:Opcode $0x1, HOLE:Mem,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (roll:Opcode $0x1, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      loadFromMemory( MemOff, 32) ~>
      execinstr (roll $0x1, memOffset( MemOff),  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> execinstr (roll:Opcode $0x1, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      
            storeToMemory(
              rol( Mem32, mi(32, 1)),
              MemOff,
              32
            )
          
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
"CF" |-> extractMInt( rol( Mem32, mi(32, 1)), 31, 32)

"OF" |-> (#ifMInt (eqMInt( extractMInt( rol( Mem32, mi(32, 1)), 0, 1), mi(1, 1)) xorBool eqMInt( extractMInt( rol( Mem32, mi(32, 1)), 31, 32), mi(1, 1))) #then mi(1, 1) #else mi(1, 0) #fi)
      )
    </regstate>
endmodule
