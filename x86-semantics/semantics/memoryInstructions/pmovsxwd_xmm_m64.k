// Autogenerated using stratification.
requires "x86-configuration.k"

module PMOVSXWD-XMM-M64
  imports X86-CONFIGURATION

  context execinstr(pmovsxwd:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (pmovsxwd:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      loadFromMemory( MemOff, 64) ~>
      execinstr (pmovsxwd memOffset( MemOff), R2:Xmm,  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem64:MInt):MemLoadValue ~> execinstr (pmovsxwd:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( extractMInt( getParentValue(R2, RSMap), 0, 128), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem64, 0, 16))), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem64, 16, 32))), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem64, 32, 48))), mi(32, svalueMInt(extractMInt( Mem64, 48, 64)))))))
      )
    </regstate>
endmodule
