// Autogenerated using stratification.
requires "x86-configuration.k"

module MOVSLDUP-XMM-M128
  imports X86-CONFIGURATION

  context execinstr(movsldup:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (movsldup:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      loadFromMemory( MemOff, 128) ~>
      execinstr (movsldup memOffset( MemOff), R2:Xmm,  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem128:MInt):MemLoadValue ~> execinstr (movsldup:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( extractMInt( getParentValue(R2, RSMap), 0, 128), concatenateMInt( concatenateMInt( extractMInt( Mem128, 32, 64), extractMInt( Mem128, 32, 64)), concatenateMInt( extractMInt( Mem128, 96, 128), extractMInt( Mem128, 96, 128))))
      )
    </regstate>
endmodule
