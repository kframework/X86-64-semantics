// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVNPW-R16-R16
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovnpw R1:R16, R2:R16,  .Typedoperands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> ((#ifMInt (eqMInt(concatenateMInt(mi(63, 0), getFlag("PF", RSMap)), mi(64, 0)) ) #then ( concatenateMInt(extractMInt(getParentValue(R2, RSMap), 0, 48), extractMInt(getParentValue(R1, RSMap), 48, 64)) ) #else ( getParentValue(R2, RSMap) ) #fi)  )


)

    </regstate>
endmodule

module CMOVNPW-R16-R16-SEMANTICS
  imports CMOVNPW-R16-R16
endmodule
/*
TargetInstr:
cmovnpw %cx, %bx
RWSet:
maybe read:{ %cx %pf }
must read:{ %cx %pf }
maybe write:{ %bx }
must write:{ }
maybe undef:{ }
must undef:{ }
required flags:{ cmov }

Circuit:
circuit:movzwl %cx, %esp    #  1     0    3      OPC=movzwl_r32_r16
circuit:cmovpol %esp, %ebx  #  2     0x3  3      OPC=cmovpol_r32_r32
BVF:
WARNING: No live out values provided, assuming { }
WARNING: No def in values provided; assuming { %mxcsr::rc[0] }
Target

cmovnpw %cx, %bx

  maybe read:      { %cx %pf }
  must read:       { %cx %pf }
  maybe write:     { %bx }
  must write:      { }
  maybe undef:     { }
  must undef:      { }
  required flags:  { cmov }

Circuits:

%rbx   : %rbx[63:16] ∘ ((%pf ? 0x1₆₄ : 0x0₆₄) = 0x0₆₄ ? %rcx[15:0] : %rbx[15:0])

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv

*/