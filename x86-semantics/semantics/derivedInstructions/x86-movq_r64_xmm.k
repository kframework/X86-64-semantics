// Autogenerated using stratification.
requires "x86-configuration.k"

module MOVQ-R64-XMM
  imports X86-CONFIGURATION

  rule <k>
    execinstr (movq R1:Xmm, R2:R64,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> (extractMInt(getParentValue(R1, RSMap), 192, 256) )


)

    </regstate>
endmodule

module MOVQ-R64-XMM-SEMANTICS
  imports MOVQ-R64-XMM
endmodule
/*
TargetInstr:
movq %xmm1, %rbx
RWSet:
maybe read:{ %xmm1 }
must read:{ %xmm1 }
maybe write:{ %rbx }
must write:{ %rbx }
maybe undef:{ }
must undef:{ }
required flags:{ sse2 }

Circuit:
circuit:vmovq %xmm1, %r10  #  1     0    5      OPC=vmovq_r64_xmm
circuit:movq %r10, %rbx    #  2     0x5  3      OPC=movq_r64_r64
BVF:
WARNING: No live out values provided, assuming { }
WARNING: No def in values provided; assuming { %mxcsr::rc[0] }
Target

movq %xmm1, %rbx

  maybe read:      { %xmm1 }
  must read:       { %xmm1 }
  maybe write:     { %rbx }
  must write:      { %rbx }
  maybe undef:     { }
  must undef:      { }
  required flags:  { sse2 }

Circuits:

%rbx   : %ymm1[63:0]

sigfpe  : sigfpe
sigbus  : sigbus
sigsegv : sigsegv

*/