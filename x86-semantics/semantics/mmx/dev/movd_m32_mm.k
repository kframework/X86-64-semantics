// Autogenerated using stratification.
requires "x86-configuration.k"

module MOVD-M32-MM
  imports X86-CONFIGURATION

  context execinstr(movd:Opcode R1:Mm, HOLE:Mem,  .Operands) [result(MemOffset)]
  
  rule <k>
     execinstr (movd:Opcode R1:Mm, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      
            storeToMemory(
              extractMInt( getParentValue(R1, RSMap), 31, 64),
              MemOff,
              32
            )
          
  ...</k>
    <regstate> RSMap:Map </regstate>
endmodule
