// Autogenerated using stratification.
requires "x86-configuration.k"

module VPMASKMOVQ-M128-XMM-XMM
  imports X86-CONFIGURATION

  context execinstr(vpmaskmovq:Opcode R1:Xmm, R2:Xmm, HOLE:Mem,  .Operands) [result(MemOffset)]
  
          rule <k>
            execinstr (vpmaskmovq:Opcode R1:Xmm, R2:Xmm, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
              loadFromMemory( MemOff, 128) ~> execinstr (vpmaskmovq R1:Xmm, R2:Xmm, memOffset( MemOff),  .Operands)
          ...</k>
          
  rule <k>
    memLoadValue(Mem128:MInt):MemLoadValue ~> execinstr (vpmaskmovq:Opcode R1:Xmm, R2:Xmm, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      
            storeToMemory(
              concatenateMInt( (#ifMInt eqMInt( extractMInt( getParentValue(R2, RSMap), 128, 129), mi(1, 1)) #then extractMInt( getParentValue(R1, RSMap), 128, 192) #else extractMInt( Mem128, 0, 64) #fi), (#ifMInt eqMInt( extractMInt( getParentValue(R2, RSMap), 192, 193), mi(1, 1)) #then extractMInt( getParentValue(R1, RSMap), 192, 256) #else extractMInt( Mem128, 64, 128) #fi)),
              MemOff,
              128
            )
          
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,

      )
      </regstate>
endmodule
