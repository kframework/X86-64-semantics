// Autogenerated using stratification.
requires "x86-configuration.k"

module PMOVSXBD-XMM-M32
  imports X86-CONFIGURATION

  context execinstr(pmovsxbd:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
          rule <k>
            execinstr (pmovsxbd:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
              loadFromMemory( MemOff, 32) ~> execinstr (pmovsxbd memOffset( MemOff), R2:Xmm,  .Operands)
          ...</k>
          
  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> execinstr (pmovsxbd:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( extractMInt( getParentValue(R2, RSMap), 0, 128), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem32, 0, 8))), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem32, 8, 16))), concatenateMInt( mi(32, svalueMInt(extractMInt( Mem32, 16, 24))), mi(32, svalueMInt(extractMInt( Mem32, 24, 32)))))))
      )
      </regstate>
endmodule
