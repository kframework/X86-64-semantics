// Autogenerated using stratification.
requires "x86-configuration.k"

module SETNP-M8
  imports X86-CONFIGURATION

  context execinstr(setnp:Opcode HOLE:Mem,  .Operands) [result(MemOffset)]
  
  rule <k>
     execinstr (setnp:Opcode memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      
            storeToMemory(
              (#ifMInt (notBool eqMInt(getFlag("PF", RSMap), mi(1,1))) #then mi(8, 1) #else mi(8, 0) #fi),
              MemOff,
              8
            )
          
  ...</k>
  
endmodule
