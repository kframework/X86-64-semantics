// Autogenerated using stratification.
requires "x86-configuration.k"

module BSRL-R32-R32
  imports X86-CONFIGURATION

  context execinstr(bsrl:Opcode HOLE:Mem,  R2:R32, .Operands) [result(MemOffset)]

  rule <k>
    execinstr (bsrl memOffset( MemOff:MInt):MemOffset, R2:R32,  .Operands) =>
      loadFromMemory( MemOff, 32) ~>
      execinstr (bsrl memOffset( MemOff), R2,  .Operands)  
  ...</k>

  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> 
      execinstr (bsrl memOffset( MemOff:MInt):MemOffset, R2:R32,  .Operands) => .
  ...</k>
   <regstate>
RSMap:Map => updateMap(RSMap,
  convToRegKeys(R2) |-> (undefMInt)
  "ZF" |-> mi(1,1)
  "SF" |-> (undefMInt)
  "CF" |-> (undefMInt)
  "OF" |-> (undefMInt)
  "PF" |-> (undefMInt)
  "AF" |-> (undefMInt)
  )
   </regstate>
    requires eqMInt(Mem32, mi(32, 0))


  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> 
      execinstr (bsrl memOffset( MemOff:MInt):MemOffset, R2:R32,  .Operands) => .
  ...</k>
   <regstate>
RSMap:Map => updateMap(RSMap,
  convToRegKeys(R2) |-> mi(32, scanReverse(Mem32, 0, 32))
  "ZF" |-> mi(1,0)
  "SF" |-> (undefMInt)
  "CF" |-> (undefMInt)
  "OF" |-> (undefMInt)
  "PF" |-> (undefMInt)
  "AF" |-> (undefMInt)
  )
   </regstate>
    requires notBool eqMInt(Mem32, mi(32, 0))

    
endmodule
