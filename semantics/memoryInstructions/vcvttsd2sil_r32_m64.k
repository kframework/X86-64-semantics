// Autogenerated using stratification.
requires "x86-configuration.k"

module VCVTTSD2SIL-R32-M64
  imports X86-CONFIGURATION

  rule <k>
    execinstr (vcvttsd2si:Opcode M:Mem, R2:R32,  .Operands) => execinstr (vcvttsd2sil:Opcode M:Mem, R2:R32,  .Operands)
  ...</k>

  context execinstr(vcvttsd2sil:Opcode HOLE:Mem, R2:R32,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (vcvttsd2sil:Opcode memOffset( MemOff:MInt):MemOffset, R2:R32,  .Operands) =>
      loadFromMemory( MemOff, 64) ~>
      execinstr (vcvttsd2sil memOffset( MemOff), R2,  .Operands)
  ...</k>
    <regstate> RSMap:Map </regstate>
          
  rule <k>
    memLoadValue(Mem64:MInt):MemLoadValue ~> execinstr (vcvttsd2sil:Opcode memOffset( MemOff:MInt):MemOffset, R2:R32,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(32, 0), cvt_double_to_int32_truncate(Mem64))
      )
    </regstate>
endmodule
