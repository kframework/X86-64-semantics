// Autogenerated using stratification.
requires "x86-configuration.k"

module VPMOVSXBQ-XMM-M16
  imports X86-CONFIGURATION

  context execinstr(vpmovsxbq:Opcode HOLE:Mem, R2:Xmm,  .Operands) [result(MemOffset)]
  
          rule <k>
            execinstr (vpmovsxbq:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
              loadFromMemory( MemOff, 16) ~> execinstr (vpmovsxbq memOffset( MemOff), R2:Xmm,  .Operands)
          ...</k>
          
  rule <k>
    memLoadValue(Mem16:MInt):MemLoadValue ~> execinstr (vpmovsxbq:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(128, 0), concatenateMInt( mi(64, svalueMInt(extractMInt( Mem16, 0, 8))), mi(64, svalueMInt(extractMInt( Mem16, 8, 16)))))
      )
      </regstate>
endmodule
