// Autogenerated using stratification.
requires "x86-configuration.k"

module MOVSL
  imports X86-CONFIGURATION

  rule <k>
    execinstr (movsl  M1:Mem, M2:Mem,  .Operands) =>
      loadFromMemory( getRegisterValue(%rsi, RSMap), 32) ~> execinstr (movsl  .Operands)
    ...</k>
    <regstate> RSMap:Map </regstate>

  
  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> execinstr (movsl  .Operands) => 
      storeToMemory(Mem32, getRegisterValue(%rdi, RSMap), 32) ~>
      setRegisterValue(addMInt(getRegisterValue(%rdi, RSMap), mi( 64, 4)), %rdi) ~>
      setRegisterValue(addMInt(getRegisterValue(%rsi, RSMap), mi( 64, 4)), %rsi)
  ...</k>
    <regstate> RSMap:Map </regstate>
      requires eqMInt({RSMap["DF"]}:>MInt, mi(1, 0))

  rule <k>
    memLoadValue(Mem32:MInt):MemLoadValue ~> execinstr (movsl  .Operands) => 
      storeToMemory(Mem32, getRegisterValue(%rdi, RSMap), 32) ~>
      setRegisterValue(subMInt(getRegisterValue(%rdi, RSMap), mi( 64, 4)), %rdi) ~>
      setRegisterValue(subMInt(getRegisterValue(%rsi, RSMap), mi( 64, 4)), %rsi)
  ...</k>
    <regstate> RSMap:Map </regstate>
      requires eqMInt({RSMap["DF"]}:>MInt, mi(1, 1))

  endmodule
