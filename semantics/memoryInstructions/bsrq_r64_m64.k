// Autogenerated using stratification.
requires "x86-configuration.k"

module BSRQ-R64-R64
  imports X86-CONFIGURATION

  context execinstr(bsrq:Opcode HOLE:Mem,  R2:R64, .Operands) [result(MemOffset)]

  rule <k>
    execinstr (bsrq memOffset( MemOff:MInt):MemOffset, R2:R64,  .Operands) =>
      loadFromMemory( MemOff, 64) ~>
      execinstr (bsrq memOffset( MemOff), R2,  .Operands)  
  ...</k>

  rule <k>
    memLoadValue(Mem64:MInt):MemLoadValue ~> 
      execinstr (bsrq memOffset( MemOff:MInt):MemOffset, R2:R64,  .Operands) => .
  ...</k>
   <regstate>
RSMap:Map => updateMap(RSMap,
  convToRegKeys(R2) |-> (undefMInt)
  "ZF" |-> mi(1,1)
  "SF" |-> (undefMInt)
  "CF" |-> (undefMInt)
  "OF" |-> (undefMInt)
  "PF" |-> (undefMInt)
  "AF" |-> (undefMInt)
  )
   </regstate>
    requires eqMInt(Mem64, mi(64, 0))


  rule <k>
    memLoadValue(Mem64:MInt):MemLoadValue ~> 
      execinstr (bsrq memOffset( MemOff:MInt):MemOffset, R2:R64,  .Operands) => .
  ...</k>
   <regstate>
RSMap:Map => updateMap(RSMap,
  convToRegKeys(R2) |-> mi(64, scanReverse(Mem64, 0, 64))
  "ZF" |-> mi(1,0)
  "SF" |-> (undefMInt)
  "CF" |-> (undefMInt)
  "OF" |-> (undefMInt)
  "PF" |-> (undefMInt)
  "AF" |-> (undefMInt)
  )
   </regstate>
    requires notBool eqMInt(Mem64, mi(64, 0))

    
endmodule
