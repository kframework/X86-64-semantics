// Autogenerated using stratification.
requires "x86-configuration.k"

module VMOVAPS-M256-YMM
  imports X86-CONFIGURATION

  context execinstr(vmovaps:Opcode R1:Ymm, HOLE:Mem,  .Operands) [result(MemOffset)]
  
          rule <k>
            execinstr (vmovaps:Opcode R1:Ymm, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
              loadFromMemory( MemOff, 256) ~> execinstr (vmovaps R1:Ymm, memOffset( MemOff),  .Operands)
          ...</k>
          
  rule <k>
    memLoadValue(Mem256:MInt):MemLoadValue ~> execinstr (vmovaps:Opcode R1:Ymm, memOffset( MemOff:MInt):MemOffset,  .Operands) =>
      
            storeToMemory(
              getParentValue(R1, RSMap),
              MemOff,
              256
            )
          
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,

      )
      </regstate>
endmodule
