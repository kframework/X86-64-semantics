// Autogenerated using stratification.
requires "x86-configuration.k"

module VUNPCKLPS-XMM-XMM-M128
  imports X86-CONFIGURATION

  context execinstr(vunpcklps:Opcode HOLE:Mem, R2:Xmm, R3:Xmm,  .Operands) [result(MemOffset)]
  
  rule <k>
    execinstr (vunpcklps:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm, R3:Xmm,  .Operands) =>
      loadFromMemory( MemOff, 128) ~>
      execinstr (vunpcklps memOffset( MemOff), R2:Xmm, R3:Xmm,  .Operands)
  ...</k>
          
  rule <k>
    memLoadValue(Mem128:MInt):MemLoadValue ~> execinstr (vunpcklps:Opcode memOffset( MemOff:MInt):MemOffset, R2:Xmm, R3:Xmm,  .Operands) =>
      .
  ...</k>
    <regstate>
      RSMap:Map => updateMap(RSMap,
convToRegKeys(R3) |-> concatenateMInt( mi(128, 0), concatenateMInt( concatenateMInt( concatenateMInt( extractMInt( Mem128, 64, 96), extractMInt( getParentValue(R2, RSMap), 192, 224)), extractMInt( Mem128, 96, 128)), extractMInt( getParentValue(R2, RSMap), 224, 256)))
      )
    </regstate>
endmodule
