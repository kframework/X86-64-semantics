// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVGEQ-R64-R64
  imports X86-CONFIGURATION

   rule <k>
    execinstr (cmovge R1:R64, R2:R64,  .Operands) => execinstr (cmovgeq R1:R64, R2:R64,  .Operands)
  ...</k>

  rule <k>
    execinstr (cmovgeq R1:R64, R2:R64,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> (#ifMInt (eqMInt(getFlag("SF", RSMap), mi(1,1)) ==Bool eqMInt(getFlag("OF", RSMap), mi(1,1))) #then getParentValue(R1, RSMap) #else getParentValue(R2, RSMap) #fi)
)

    </regstate>
    
endmodule

module CMOVGEQ-R64-R64-SEMANTICS
  imports CMOVGEQ-R64-R64
endmodule
