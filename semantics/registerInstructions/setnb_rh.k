// Autogenerated using stratification.
requires "x86-configuration.k"

module SETNB-RH
  imports X86-CONFIGURATION

  rule <k>
    execinstr (setnb R1:Rh,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R1) |-> concatenateMInt( concatenateMInt( extractMInt( getParentValue(R1, RSMap), 0, 48), (#ifMInt eqMInt( (#ifMInt eqMInt(getFlag("CF", RSMap), mi(1,1)) #then mi(1, 1) #else mi(1, 0) #fi), mi(1, 1)) #then mi(8, 0) #else mi(8, 1) #fi)), extractMInt( getParentValue(R1, RSMap), 56, 64))
)

    </regstate>
endmodule

module SETNB-RH-SEMANTICS
  imports SETNB-RH
endmodule
