// Autogenerated using stratification.
requires "x86-configuration.k"

module CMOVNZL-R32-R32
  imports X86-CONFIGURATION

  rule <k>
    execinstr (cmovnzl R1:R32, R2:R32,  .Operands) => .
  ...</k>
    <regstate>
RSMap:Map => updateMap(RSMap,
convToRegKeys(R2) |-> concatenateMInt( mi(32, 0), (#ifMInt eqMInt(getFlag("ZF", RSMap), mi(1,1)) #then extractMInt( getParentValue(R2, RSMap), 32, 64) #else extractMInt( getParentValue(R1, RSMap), 32, 64) #fi))
)

    </regstate>
endmodule

module CMOVNZL-R32-R32-SEMANTICS
  imports CMOVNZL-R32-R32
endmodule
